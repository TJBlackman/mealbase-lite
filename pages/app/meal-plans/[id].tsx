import { Typography } from '@mui/material';

type Props = {
  id: string;
};

export default function MealPlanDetailsPage(props: Props) {
  console.log(props);
  return <Typography>Meal Plan Details page</Typography>;
}
